<div class="owl-demo owl-carousel">
	<% if (post.photos.length){ %>
		<% post.photos.forEach(function(photo){ %>
			<div class="item">
				<img src="<%- photo %>" />
			</div>
		<% }); %>
	<% } else { %>
		<div class="item">
			<img src="<%- post.photos %>" />
		</div>
	<% } %>
</div>